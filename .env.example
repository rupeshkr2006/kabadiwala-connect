# Optional server-side enhancement. The app works without this key by using
# browser speech recognition + deterministic multilingual extraction.
GEMINI_API_KEY=

# Optional model name. Keep the default unless your provider exposes another model.
GEMINI_MODEL=gemini-2.5-flash
